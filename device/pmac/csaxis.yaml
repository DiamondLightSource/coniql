use:
  type: protocol.channel.ca.CaBool
  pv: $(prefix):UseAxis

num_points:
  type: protocol.channel.ca.CaInt
  pv: $(prefix):NoOfPts

max_points:
  type: protocol.channel.ca.CaInt
  pv: $(prefix):Positions.NELM

positions:
  type: protocol.channel.ca.CaFloatArray
  pv: $(prefix):Positions
