position_capture:
  type: device.zebra.PositionCapture
  prefix: $(prefix)

pulse_1:
  type: device.zebra.Pulse
  prefix: $(prefix)
  num: 1

io:
  type: device.zebra.ZebraIO
  prefix: $(prefix)

system_reset:
  type: device.channel.ca.CaBool
  pv: $(prefix):SYS_RESET.PROC
